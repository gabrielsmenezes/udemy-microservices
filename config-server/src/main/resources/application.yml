server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gabrielsmenezes/microservices-repo
          search-paths: [greeting-service]